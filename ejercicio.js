class Estudiante {
    constructor(nombre, edad, listaAsignatura ) {
        this.nombre = nombre
        this.edad = edad
       this.listaAsignatura = listaAsignatura[{

       }
      
      
       ]
       
    }

    obtenerDatos() {
        console.log(`${this.nombre}, ${this.edad} ${this.listaAsignatura}`)
    }
}

const nueva_estudiante = new Estudiante("Gorka", 34 ,{nombre: html})
console.log(nueva_estudiante)
nueva_estudiante.obtenerDatos()